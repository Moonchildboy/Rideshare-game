pseudo.js

// grind: function () { //stage method
// 		// for every instance "trips" is less than 1 ++ until trips is equal to 10
// 		while (this.trips < 10){
			// const input = prompt ("Accept trip? (yes/no)") 
			// if (input === "yes") {
			// 	this.trips += 1
// 				console.log(this.trips);
// 				if (this.trips === 10){
// 					this.days += 1
// 					console.log(this.days);
// 				}
// 			}
// 		} 
// 	}//End of 1st Method
// try drying the above with a for loop. 



for (let i = 0; i < 50; i++) {
	const input = prompt ("Accept trip? (yes/no)") 
	if (input === "yes") {
		this.trips += 1
		if (this.trips 10 % === 0) {
			this.days +=1 
		} 
	}	
}

// call game object